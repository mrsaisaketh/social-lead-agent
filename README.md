# Social to Lead Agent

## Run
pip install -r requirements.txt
python main.py

## Features
- Intent Detection
- RAG Knowledge Base
- Lead Capture Tool
- Stateful Conversation

## Architecture
Uses modular structure with intent classification, retrieval system, and tool execution. State is maintained using in-memory dictionary.
